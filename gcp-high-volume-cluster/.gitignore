# Environment files
.env

# Generated Kubernetes manifests
k8s/apps.yaml
k8s/loadgens.yaml

# Backup files (may contain sensitive data)
backups/

# Support logs (may contain sensitive data in URLs)
support-logs-*.txt
*-logs-*.txt
*-LOGS.txt
HEALTHY-STATUS-LOGS.txt

# Notes and documentation (work in progress)
notes/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars
terraform/service-account-key.json
*service-account*.json

# Maven
app/target/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
service-account-key.json
